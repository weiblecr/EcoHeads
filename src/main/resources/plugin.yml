name: ${name}
version: ${version}
description: ${description}
author: ${author}
website: ${url}

main: ${mainClass}

commands:
    ecoheads:
        usage: /ecoheads.ecoheads
        description: Economy Based Access to HeadsDB
        aliases: [eh,ehdb]

permissions:
  ecoheaddb.open:
    default: true
  ecoheaddb.search:
    default: true
    
softdepend: [HeadDB, Vault, Essentials]